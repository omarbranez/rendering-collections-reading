<%# @posts.each do |post| %>
  <%# render :partial => "post", locals: {:post => post} %> 
  <%# render :partial => "post", :collection => @posts %>
  <%= render @posts %>
  <%# will iterate over a collection of posts in the post partial %>
<%# end %>